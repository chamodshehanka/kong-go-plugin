_format_version: "2.1"
_transform: true

services:
- name: hello
  url: https://hello.com
  routes:
  - name: hello
    paths:
    - /
    preserve_host: false
    strip_path: false

plugins:
- name: hello-plugin
  config:
    message: hello
